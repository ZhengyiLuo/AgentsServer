[project]
name = "agents-server-runtime"
version = "0.1.2"
requires-python = ">=3.10"
dependencies = [
  "cryptography>=44,<47",
  "fastapi>=0.115,<1",
  "pydantic>=2.10,<3",
  "python-multipart>=0.0.20,<1",
  "uvicorn>=0.34,<1",
]

[tool.uv]
package = false
